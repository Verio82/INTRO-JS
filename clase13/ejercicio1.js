//Crea una funcion declarada que reciba la base y la altura del triangulo y retorne el area del mismo
function calcularAreaTriangulo(base, altura) {
    let area = (base * altura) / 2;
    return area;    
}
